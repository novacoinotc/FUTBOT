{
  "name": "botsurviver",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "db:generate": "pnpm --filter server drizzle-kit generate",
    "db:migrate": "pnpm --filter server drizzle-kit migrate",
    "db:seed": "pnpm --filter server tsx src/db/seed.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
